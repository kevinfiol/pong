-- game variables
return {
    mouse = { x = 0, y = 0 },
    gw = 480, -- base resolution width
    gh = 272, -- base resolution height
    sx = 1, -- scale applied to base resolution x axis
    sy = 1 -- scale applied to base resolution y axis
}